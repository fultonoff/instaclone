/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.dhphoto.se', 'upload.wikimedia.org', "freelogopng.com"],
  },
}

module.exports = nextConfig
